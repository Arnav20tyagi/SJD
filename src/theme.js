// src/theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: `"hindi", sans-serif, "windhavi", "maharani-r"`,
  },
});

export default theme;
